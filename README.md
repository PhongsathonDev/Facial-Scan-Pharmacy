## 💊 Face-Verified Automatic Drug Dispenser (ระบบจ่ายยาอัตโนมัติด้วยการสแกนใบหน้า)

### 📝 คำอธิบายโครงการ

โครงการนี้คือ **ระบบจดจำใบหน้า** สำหรับการควบคุมการทำงานของเครื่องจ่ายยาอัตโนมัติ โดยใช้ **Raspberry Pi 5** เป็นบอร์ดหลัก ระบบจะอนุญาตให้จ่ายยาได้ก็ต่อเมื่อมีการสแกนและยืนยันใบหน้าผู้ใช้ที่ลงทะเบียนไว้เรียบร้อยแล้ว โดยต้องมองตรงเข้าหากล้องค้างไว้เป็นระยะเวลาที่กำหนด เพื่อป้องกันความผิดพลาดและเพิ่มความปลอดภัย

นอกจากนี้ ระบบยังสามารถ**บันทึกข้อมูลการสแกน**ไปยัง **Google Sheet** ผ่าน Google Apps Script Web App ได้อีกด้วย ทำให้สามารถติดตามการใช้งานหรือตรวจสอบประวัติได้ง่ายขึ้น

### ⚙️ คุณสมบัติหลัก

  * **การจดจำใบหน้า:** ใช้ไลบรารี `face_recognition` ที่มีความแม่นยำสูง
  * **การยืนยันใบหน้า (Liveness Check/Hold Time):** ต้องตรวจพบบุคคลที่รู้จักอย่างต่อเนื่องเป็นระยะเวลาหนึ่ง (เช่น 3 วินาที) ก่อนจะถือว่า "ผ่าน" เพื่อป้องกันการใช้ภาพนิ่ง
  * **แสดงผลวิดีโอสด:** แสดงภาพจากกล้องพร้อมกรอบสี่เหลี่ยมระบุใบหน้าและสถานะการจดจำ
  * **การบันทึกข้อมูล:** ส่งบันทึกการยืนยันใบหน้าสำเร็จไปยัง **Google Sheet** (ต้องตั้งค่า Web App URL)
  * **ใช้งานบน Raspberry Pi 5:** เหมาะสำหรับการติดตั้งเป็นอุปกรณ์จริง

### 🛠️ ความต้องการ (Requirements)

  * **ฮาร์ดแวร์:**
      * Raspberry Pi 5
      * กล้อง USB หรือกล้อง CSI ที่รองรับ (ตั้งค่า `camera_index` ให้ถูกต้อง)
      * (ส่วนที่เชื่อมต่อกับโค้ดจริง): กลไกจ่ายยาที่ถูกกระตุ้นเมื่อการยืนยันใบหน้าสำเร็จ (ไม่ได้อยู่ในโค้ดตัวอย่างนี้)
  * **ซอฟต์แวร์/ไลบรารี Python:**
      * `Python 3`
      * `face_recognition`
      * `opencv-python` (`cv2`)
      * `numpy`
      * `requests`

<!-- end list -->

```bash
# ติดตั้งไลบรารีที่จำเป็น
pip install face_recognition opencv-python numpy requests
```

### 🚀 วิธีการใช้งาน

#### 1\. การเตรียมภาพต้นแบบ

เตรียมรูปภาพของผู้ใช้ที่ต้องการให้ระบบรู้จัก (ใบหน้าเดียว) และบันทึกไว้ เช่น **`paper.jpeg`**

#### 2\. การตั้งค่า Google Sheet (Optional)

1.  สร้าง Google Sheet ใหม่
2.  สร้าง Google Apps Script Web App ที่รับการร้องขอ POST เพื่อบันทึกข้อมูลลงใน Sheet นั้น
3.  นำ URL ของ Web App มากำหนดในตัวแปร `WEBAPP_URL`

#### 3\. การตั้งค่าโค้ด

ปรับปรุงโค้ดในส่วน `if __name__ == "__main__":`

```python
if __name__ == "__main__":
    WEBAPP_URL = "https://your-google-apps-script-web-app-url/exec" # แก้ตรงนี้

    verifier = FaceVerifier(
        known_image_path="paper.jpeg", # แก้ชื่อไฟล์รูปต้นแบบ
        known_name="Paper",          # แก้ชื่อผู้ใช้
        tolerance=0.45,              # ค่าความเข้มงวด (0.45 = มาตรฐานที่ดี)
        hold_seconds=3.0,            # จำนวนวินาทีที่ต้องมองค้าง
        camera_index=0,              # Index กล้อง (0 มักจะเป็นกล้องหลัก)
        webapp_url=WEBAPP_URL,
        sheet_name="sheet1",         # ชื่อชีตใน Google Sheet
        face_id="user_001"           # ID ผู้ใช้
    )
    verifier.run()
```

#### 4\. การรันโปรแกรม

```bash
python your_script_name.py
```

เมื่อโปรแกรมเปิดขึ้น:

1.  หน้าต่างวิดีโอจะปรากฏขึ้น
2.  **มองตรง**เข้าหากล้อง เมื่อระบบรู้จักใบหน้า จะเริ่มนับเวลา **Hold still**
3.  หากมองค้างไว้ครบตามเวลาที่กำหนด (เช่น 3.0 วินาที) สถานะจะเปลี่ยนเป็น **"Face Verified"**
4.  ข้อมูลจะถูกส่งไปยัง Google Sheet และโปรแกรมจะหยุดทำงาน (ในระบบจริง สามารถเพิ่มโค้ดสั่งจ่ายยาได้ในจุดนี้)
5.  กด **'q'** เพื่อยกเลิกโปรแกรม

### 💡 แนวทางการต่อยอด

  * **การควบคุมฮาร์ดแวร์:** เพิ่มโค้ดควบคุม GPIO ของ Raspberry Pi เพื่อสั่งให้มอเตอร์หรือโซลินอยด์ทำงานเพื่อ**จ่ายยา**เมื่อการยืนยันใบหน้าสำเร็จ (หลังบรรทัด `self.send_log_to_sheet(...)` ในเมธอด `_update_hold_state`)
  * **ผู้ใช้หลายคน:** ปรับปรุงโค้ดให้รองรับการโหลดภาพต้นแบบและข้อมูลของผู้ใช้หลายคนจากไฟล์/ฐานข้อมูล

-----

หากต้องการให้ฉันช่วยต่อยอดโค้ดในส่วนของการสั่งงานฮาร์ดแวร์ของ Raspberry Pi 5 หรือส่วนอื่น ๆ บอกได้เลยนะคะ\! 🌟
